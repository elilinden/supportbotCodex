# Required
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: API key auth (if set, clients must send x-api-key header)
SERVER_API_KEY=

# Optional: Comma-separated allowed CORS origins
CORS_ORIGINS=http://localhost:3000,chrome-extension://

# Optional: Server port (default: 8787)
PORT=8787

# Optional: Set to "true" to use mock LLM responses (for testing)
MOCK_LLM=
