{
  "name": "pro-se-prime",
  "version": "1.0.0",
  "private": true,
  "description": "NY Family Court OP Navigator — monorepo with web app, server, and extension",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=web && npm test --workspace=server",
    "lint": "npm run lint --workspace=web && npm run lint --workspace=server"
  },
  "engines": {
    "node": ">=18"
  }
}
